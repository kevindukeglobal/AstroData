# AstroData

TESS transit recovery pipeline. Work in progress.

## Status

Rebuilt from scratch. Nothing here is a published result yet.

## Structure

- `scripts/` reduction and query scripts
- `notebooks/` exploration
- `data/` downloaded FITS products (gitignored)
- `outputs/` generated figures and tables (gitignored by default)
